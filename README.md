# My PHP Docker Login Project

This is a simple login system built with Nginx, PHP-FPM, and MySQL running on Docker.

## CI Status

The CI pipeline is active and builds the PHP Docker image on every push to the master branch.
